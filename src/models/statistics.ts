export class Statistics {
  waza: [string, number][];
  tokusei: [string, number][];
  seikaku: [string, number][];

  constructor() {
    this.waza = [['なきごえ', 100.0]];
    this.tokusei = [['いかく', 100.0]];
    this.seikaku = [['ようき', 100.0]];
  }
}
